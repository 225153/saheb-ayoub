/* ==========================================================================
   NOOH Boutique — i18n (AR default + RTL / EN / FR) + Product Engine
========================================================================== */
const i18n = {
  ar: {
    announcement:
      "شحن سريع مجاني حول العالم للطلبات فوق 300$ • إصدار الطبيعة 2026",
    brand_sub: "بوتيك",
    nav_gallery: "المعرض",
    nav_collections: "المجموعات",
    nav_silk: "حرير وساتان",
    nav_pearlcare: "العناية باللؤلؤ",
    nav_bespoke: "حسب الطلب",
    hero_tag: "إصدار الطبيعة 2026",
    hero_title: "معرض احترافي مصقول",
    hero_desc:
      "تشكيلة بورتريه حالمة تجمع بين بريق اللؤلؤ العضوي، وخامات المخمل الفاخرة، وقصّات انسيابية بحدٍّ أدنى من التفاصيل.",
    pill_all: "كل القطع",
    pill_jewelry: "مجوهرات اللؤلؤ",
    pill_silk: "حرير وساتان",
    pill_tailored: "بلازرات مفصّلة",
    pill_accessories: "إكسسوارات",
    pill_care: "العناية باللؤلؤ",
    showing: "عرض {n} من التحف المنسّقة",
    showing_one: "عرض تحفة واحدة منسّقة",
    sort_featured: "ترتيب مميز",
    sort_price_low: "السعر: من الأقل إلى الأعلى",
    sort_price_high: "السعر: من الأعلى إلى الأقل",
    sort_newest: "أحدث الوصلات",
    empty_title: "لا توجد قطع في المعرض",
    empty_desc: "جرّب تعديل عبارة البحث أو اختيار تصنيف آخر.",
    reset_filters: "إعادة تعيين الكل",
    sale: "تخفيض",
    quick_view: "عرض سريع",
    want_purchase: "أرغب بالشراء",
    select_finish: "اختر اللمعة / اللون",
    cart_title: "حقيبة تسوقك",
    subtotal: "المجموع الفرعي",
    checkout_note: "تُحتسب الضرائب والشحن السريع حول العالم عند إتمام الطلب.",
    checkout_btn: "إتمام الطلب",
    remove: "إزالة",
    cart_empty: "حقيبة تسوقك فارغة حالياً.",
    search_placeholder: "ابحث عن اللؤلؤ، الحرير، البلازرات…",
    loading_products: "جارٍ تحميل القطع…",
    load_error: "تعذّر تحميل القطع من المتجر.",
    footer_desc:
      "مفهوم متجر أثيري يجمع لآلئ الباروك المستوحاة من الطبيعة مع جماليات المخمل ذات الفخامة الهادئة.",
    col_collections: "المجموعات",
    col_atelier: "المشغل",
    col_newsletter: "النشرة البريدية",
    news_desc: "اشترك للحصول على معاينة خاصة للإصدارات الجديدة.",
    join: "انضم",
    link_tahitian: "لآلئ تاهيتي",
    link_southsea: "لآلئ البحار الجنوبية الذهبية",
    link_baroque: "إصدارات باروك",
    link_silkvelvet: "أزياء الحرير والمخمل",
    link_philosophy: "فلسفتنا",
    link_luster: "العناية بلمعة اللؤلؤ",
    link_commissions: "طلبات خاصة",
    link_sustainability: "الاستدامة",
    footer_bottom: "© 2026 بوتيك نوح. صُمّم بمواصفات المتجر الأثيري.",
    finish_sage: "مريمية ناعمة",
    finish_carbon: "أسود كربوني",
    finish_ivory: "عاجي",
    toast_added: "أُضيفت إلى حقيبة تسوقك.",
    toast_removed_bag: "أُزيلت القطعة من الحقيبة.",
    toast_wish_add: "حُفظت القطعة في قائمة أمنياتك.",
    toast_wish_remove: "أُزيلت القطعة من قائمة أمنياتك.",
    toast_whatsapp: "جارٍ فتح واتساب مع طلب الشراء.",
    toast_newsletter: "شكراً لانضمامك إلى قائمة المشغل الخاصة.",
    wa_msg: "مرحباً! أرغب بشراء {title} — عدد {q}.",
    cat_jewelry: "مجوهرات اللؤلؤ",
    cat_silk: "حرير وساتان",
    cat_tailored: "بلازرات مفصّلة",
    cat_accessories: "إكسسوارات",
    cat_care: "العناية باللؤلؤ",
  },
  en: {
    announcement:
      "COMPLIMENTARY WORLDWIDE EXPRESS SHIPPING ON ORDERS OVER $300 • THE NATURE EDIT 2026",
    brand_sub: "BOUTIQUE",
    nav_gallery: "Gallery",
    nav_collections: "Collections",
    nav_silk: "Silk & Satin",
    nav_pearlcare: "Pearl Care",
    nav_bespoke: "Bespoke",
    hero_tag: "THE 2026 NATURE EDIT",
    hero_title: "Sharpened Professional Gallery",
    hero_desc:
      "An ethereal portrait collection bridging organic pearl brilliance, plush velvet textures, and minimalist silhouette tailoring.",
    pill_all: "All Pieces",
    pill_jewelry: "Pearl Jewelry",
    pill_silk: "Silk & Satin",
    pill_tailored: "Tailored Blazers",
    pill_accessories: "Accessories",
    pill_care: "Pearl Care",
    showing: "Showing {n} Curated Masterpieces",
    showing_one: "Showing 1 Curated Masterpiece",
    sort_featured: "Featured Edit",
    sort_price_low: "Price: Low to High",
    sort_price_high: "Price: High to Low",
    sort_newest: "Newest Arrivals",
    empty_title: "No gallery pieces found",
    empty_desc:
      "Try adjusting your search query or selecting a different category filter.",
    reset_filters: "Reset All Filters",
    sale: "SALE",
    quick_view: "Quick View",
    want_purchase: "I want to purchase",
    select_finish: "Select Luster / Finish",
    cart_title: "Your Shopping Bag",
    subtotal: "Subtotal",
    checkout_note: "Taxes & express worldwide shipping calculated at checkout.",
    checkout_btn: "Proceed to Checkout",
    remove: "Remove",
    cart_empty: "Your shopping bag is currently empty.",
    search_placeholder: "Search pearls, silk, blazers…",
    loading_products: "Loading pieces…",
    load_error: "Failed to load pieces from the store.",
    footer_desc:
      "Ethereal boutique concept combining nature-inspired baroque pearls with quiet luxury velvet aesthetics.",
    col_collections: "COLLECTIONS",
    col_atelier: "ATELIER",
    col_newsletter: "NEWSLETTER",
    news_desc: "Subscribe to receive private preview access to new edits.",
    join: "Join",
    link_tahitian: "Tahitian Pearls",
    link_southsea: "South Sea Gold",
    link_baroque: "Baroque Editions",
    link_silkvelvet: "Silk & Velvet Wear",
    link_philosophy: "Our Philosophy",
    link_luster: "Pearl Luster Care",
    link_commissions: "Custom Commissions",
    link_sustainability: "Sustainability",
    footer_bottom:
      "© 2026 NOOH Boutique. Designed with the Ethereal Shop specification.",
    finish_sage: "Soft Sage",
    finish_carbon: "Carbon Black",
    finish_ivory: "Ivory",
    toast_added: "Added to your shopping bag.",
    toast_removed_bag: "Item removed from bag.",
    toast_wish_add: "Item saved to your wishlist.",
    toast_wish_remove: "Item removed from your wishlist.",
    toast_whatsapp: "Opening WhatsApp with your purchase request.",
    toast_newsletter: "Thank you for joining our private atelier list.",
    wa_msg: "Hello! I would like to purchase {title} — {q} piece(s).",
    cat_jewelry: "Pearl Jewelry",
    cat_silk: "Silk & Satin",
    cat_tailored: "Tailored Blazers",
    cat_accessories: "Accessories",
    cat_care: "Pearl Care",
  },
  fr: {
    announcement:
      "LIVRAISON EXPRESS MONDIALE OFFERTE DÈS 300$ • L'ÉDITION NATURE 2026",
    brand_sub: "BOUTIQUE",
    nav_gallery: "Galerie",
    nav_collections: "Collections",
    nav_silk: "Soie & Satin",
    nav_pearlcare: "Soin des Perles",
    nav_bespoke: "Sur Mesure",
    hero_tag: "L'ÉDITION NATURE 2026",
    hero_title: "Galerie Professionnelle Raffinée",
    hero_desc:
      "Une collection portrait éthérée mêlant l'éclat organique des perles, des velours somptueux et une silhouette tailleur minimaliste.",
    pill_all: "Toutes les pièces",
    pill_jewelry: "Bijoux de perles",
    pill_silk: "Soie & Satin",
    pill_tailored: "Blazers tailleur",
    pill_accessories: "Accessoires",
    pill_care: "Soin des perles",
    showing: "{n} pièces d'exception présentées",
    showing_one: "1 pièce d'exception présentée",
    sort_featured: "Sélection",
    sort_price_low: "Prix : croissant",
    sort_price_high: "Prix : décroissant",
    sort_newest: "Nouveautés",
    empty_title: "Aucune pièce trouvée",
    empty_desc: "Essayez d'ajuster votre recherche ou un autre filtre.",
    reset_filters: "Réinitialiser les filtres",
    sale: "SOLDE",
    quick_view: "Aperçu rapide",
    want_purchase: "Je souhaite acheter",
    select_finish: "Choisir le lustre / fini",
    cart_title: "Votre sac",
    subtotal: "Sous-total",
    checkout_note: "Taxes et livraison express calculées au paiement.",
    checkout_btn: "Passer à la commande",
    remove: "Retirer",
    cart_empty: "Votre sac est actuellement vide.",
    search_placeholder: "Rechercher perles, soie, blazers…",
    loading_products: "Chargement des pièces…",
    load_error: "Échec du chargement des pièces.",
    footer_desc:
      "Concept de boutique éthérée mêlant perles baroques inspirées de la nature et velours au luxe discret.",
    col_collections: "COLLECTIONS",
    col_atelier: "ATELIER",
    col_newsletter: "INFOLETTRE",
    news_desc: "Abonnez-vous pour un accès privé aux nouvelles éditions.",
    join: "Rejoindre",
    link_tahitian: "Perles de Tahiti",
    link_southsea: "Or des Mers du Sud",
    link_baroque: "Éditions baroques",
    link_silkvelvet: "Soie & Velours",
    link_philosophy: "Notre philosophie",
    link_luster: "Soin du lustre",
    link_commissions: "Commandes sur mesure",
    link_sustainability: "Durabilité",
    footer_bottom:
      "© 2026 NOOH Boutique. Conçu selon la spécification Ethereal Shop.",
    finish_sage: "Sauge douce",
    finish_carbon: "Noir carbone",
    finish_ivory: "Ivoire",
    toast_added: "Ajouté à votre sac.",
    toast_removed_bag: "Article retiré du sac.",
    toast_wish_add: "Article ajouté à votre liste.",
    toast_wish_remove: "Article retiré de votre liste.",
    toast_whatsapp: "Ouverture de WhatsApp avec votre demande.",
    toast_newsletter: "Merci d'avoir rejoint notre liste privée.",
    wa_msg: "Bonjour ! Je souhaite acheter {title} — {q} pièce(s).",
    cat_jewelry: "Bijoux de perles",
    cat_silk: "Soie & Satin",
    cat_tailored: "Blazers tailleur",
    cat_accessories: "Accessoires",
    cat_care: "Soin des perles",
  },
};

/* Catalog source — loaded from Supabase (articles added via admin.html) */
let sb = null;

async function initStorefrontSupabase() {
  if (sb) return sb;
  try {
    const response = await fetch("/.netlify/functions/supabase-config");
    if (!response.ok) throw new Error("Failed to load Supabase config");
    const { url, anonKey } = await response.json();
    sb = window.supabase?.createClient
      ? window.supabase.createClient(url, anonKey)
      : null;
  } catch (err) {
    console.error(err);
    sb = null;
  }
  return sb;
}

let products = [];

const fallbackProducts = [
  {
    id: "fallback-1",
    category: "jewelry",
    price: 340,
    oldPrice: 410,
    isSale: true,
    image:
      "https://images.unsplash.com/photo-1535632066927-ab7c9ab60908?auto=format&fit=crop&w=800&q=80",
    title: "Luminous Baroque Pearl Drop Earrings",
    description:
      "Hand-selected Tahitian baroque pearls suspended from 18k solid gold studs. Naturally shaped with high iridescent luster.",
    stock: 5,
    created_at: new Date().toISOString(),
  },
  {
    id: "fallback-2",
    category: "tailored",
    price: 580,
    oldPrice: null,
    isSale: false,
    image:
      "https://images.unsplash.com/photo-1584273143981-41c073dfe8f8?auto=format&fit=crop&w=800&q=80",
    title: "Tailored Soft Sage Velvet Blazer",
    description:
      "Structured double-breasted velvet blazer in botanical soft sage. Fully silk-lined with genuine mother-of-pearl buttons.",
    stock: 3,
    created_at: new Date().toISOString(),
  },
  {
    id: "fallback-3",
    category: "silk",
    price: 460,
    oldPrice: 520,
    isSale: true,
    image:
      "https://images.unsplash.com/photo-1595777457583-95e059d581b8?auto=format&fit=crop&w=800&q=80",
    title: "Midnight Silk Slip Dress with Pearl Trim",
    description:
      "Heavyweight 100% Mulberry silk slip dress in dark carbon. Delicate freshwater pearl embellishments along the back tie.",
    stock: 4,
    created_at: new Date().toISOString(),
  },
];

/* State — Arabic is the default language */
let state = {
  lang: "ar",
  activeCategory: "all",
  searchQuery: "",
  sortBy: "featured",
  wishlist: new Set(),
  cart: [],
  selectedModalProduct: null,
};

const $ = (id) => document.getElementById(id);
const productGrid = $("product-grid"),
  emptyState = $("empty-state"),
  resultsCount = $("results-count"),
  filterPills = $("filter-pills"),
  sortSelect = $("sort-select"),
  searchInput = $("live-search-input"),
  searchOverlay = $("search-overlay"),
  searchToggleBtn = $("search-toggle-btn"),
  clearSearchBtn = $("clear-search"),
  wishlistCount = $("wishlist-count"),
  cartCount = $("cart-count"),
  cartDrawer = $("cart-drawer"),
  cartDrawerOverlay = $("cart-drawer-overlay"),
  cartBtn = $("cart-btn"),
  closeCartBtn = $("close-cart-btn"),
  cartItemsContainer = $("cart-items-container"),
  cartSubtotal = $("cart-subtotal"),
  checkoutTotal = $("checkout-total"),
  quickViewModal = $("quick-view-modal"),
  closeModalBtn = $("close-modal-btn"),
  toastContainer = $("toast-container"),
  viewGrid1 = $("view-grid-1"),
  viewGrid2 = $("view-grid-2"),
  mobileMenuToggle = $("mobile-menu-toggle"),
  mobileNavDrawer = $("mobile-nav-drawer"),
  mobileNavOverlay = $("mobile-nav-overlay"),
  closeMobileNav = $("close-mobile-nav");

function t(key) {
  return (i18n[state.lang] && i18n[state.lang][key]) || i18n.en[key] || key;
}
function loc(p) {
  return p.i18n
    ? p.i18n[state.lang] || p.i18n.en
    : { title: p.title || "", desc: p.description || "" };
}
function catName(cat) {
  return t("cat_" + cat);
}
function normalizeProductId(id) {
  return String(id);
}

/* ── Language switching (the fix!) ── */
function applyLanguage(lang) {
  state.lang = lang;
  document.documentElement.lang = lang;
  document.documentElement.dir = lang === "ar" ? "rtl" : "ltr";
  document.querySelectorAll("[data-i18n]").forEach((el) => {
    const k = el.dataset.i18n;
    if (t(k) !== k) el.textContent = t(k);
  });
  document.querySelectorAll("[data-i18n-placeholder]").forEach((el) => {
    el.placeholder = t(el.dataset.i18nPlaceholder);
  });
  document
    .querySelectorAll(".lang-btn")
    .forEach((b) => b.classList.toggle("active", b.dataset.lang === lang));
  renderGrid();
  renderCartDrawer();
}

/* ── Rendering ── */
function getFilteredProducts() {
  return products
    .filter(
      (p) =>
        (state.activeCategory === "all" ||
          p.category === state.activeCategory) &&
        (loc(p).title.toLowerCase().includes(state.searchQuery.toLowerCase()) ||
          catName(p.category)
            .toLowerCase()
            .includes(state.searchQuery.toLowerCase()) ||
          loc(p).desc.toLowerCase().includes(state.searchQuery.toLowerCase())),
    )
    .sort((a, b) => {
      if (state.sortBy === "price-low") return a.price - b.price;
      if (state.sortBy === "price-high") return b.price - a.price;
      if (state.sortBy === "newest")
        return new Date(b.created_at || 0) - new Date(a.created_at || 0);
      return 0;
    });
}

/* ── Load catalog from Supabase ── */
async function loadProducts() {
  productGrid.innerHTML = `<div class="loading-row"><i class="fa-solid fa-spinner fa-spin"></i> <span>${t("loading_products")}</span></div>`;

  await initStorefrontSupabase();
  if (!sb) {
    products = fallbackProducts;
    renderGrid();
    return;
  }

  const { data, error } = await sb
    .from("articles")
    .select("*")
    .order("created_at", { ascending: false });
  if (error) {
    products = fallbackProducts;
    showToast(
      t("load_error") + (error.message ? " (" + error.message + ")" : ""),
    );
    renderGrid();
    return;
  }
  products = (data || []).map((a) => ({
    id: normalizeProductId(a.id),
    category: a.category || "all",
    price: Number(a.price) || 0,
    oldPrice: null,
    isSale: false,
    image: a.image_url || "",
    title: a.title || "",
    description: a.description || "",
    stock: a.stock,
    created_at: a.created_at,
  }));
  if (products.length === 0) {
    products = fallbackProducts;
  }
  renderGrid();
}

function renderGrid() {
  const items = getFilteredProducts();
  resultsCount.textContent =
    items.length === 1
      ? t("showing_one")
      : t("showing").replace("{n}", items.length);
  if (items.length === 0) {
    productGrid.innerHTML = "";
    emptyState.classList.remove("hidden");
    return;
  }
  emptyState.classList.add("hidden");
  productGrid.innerHTML = items
    .map((p) => {
      const productId = normalizeProductId(p.id);
      const wished = state.wishlist.has(productId);
      return `<article class="product-card" data-id="${productId}">
      <div class="card-image-wrapper" onclick="openQuickView('${productId}', event)">
        ${p.isSale ? `<span class="badge-sale">${t("sale")}</span>` : ""}
        <button class="wishlist-card-btn ${wished ? "active" : ""}" aria-label="Wishlist" onclick="toggleWishlist('${productId}', event)">
          <i class="${wished ? "fa-solid" : "fa-regular"} fa-heart"></i>
        </button>
        <img src="${p.image}" alt="${loc(p).title}" loading="lazy" onerror="this.onerror=null;this.src='data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==';">
        <div class="quick-add-overlay">
          <button class="btn btn-primary btn-sm flex-1" onclick="openQuickView('${productId}', event)"><i class="fa-solid fa-eye"></i> ${t("quick_view")}</button>
          <button class="btn btn-rose btn-sm" onclick="quickAddToCart('${productId}', event)" title="Quick Add"><i class="fa-solid fa-plus"></i></button>
        </div>
      </div>
      <div class="card-details" onclick="openQuickView('${productId}', event)">
        <span class="card-category">${catName(p.category)}</span>
        <h3 class="card-title">${loc(p).title}</h3>
        <div class="card-price-row"><span class="card-price">$${p.price}</span>${p.oldPrice ? `<span class="card-old-price">$${p.oldPrice}</span>` : ""}</div>
      </div>
    </article>`;
    })
    .join("");
}

/* ── Wishlist / Cart ── */
window.toggleWishlist = function (id, e) {
  if (e) e.stopPropagation();
  const normalizedId = normalizeProductId(id);
  if (state.wishlist.has(normalizedId)) {
    state.wishlist.delete(normalizedId);
    showToast(t("toast_wish_remove"));
  } else {
    state.wishlist.add(normalizedId);
    showToast(t("toast_wish_add"));
  }
  updateHeaderBadges();
  renderGrid();
};
window.quickAddToCart = function (id, e) {
  if (e) e.stopPropagation();
  const normalizedId = normalizeProductId(id);
  const ex = state.cart.find((i) => i.id === normalizedId);
  if (ex) ex.qty += 1;
  else state.cart.push({ id: normalizedId, qty: 1, finish: "sage" });
  updateHeaderBadges();
  renderCartDrawer();
  showToast(t("toast_added"));
};
function updateHeaderBadges() {
  wishlistCount.textContent = state.wishlist.size;
  const q = state.cart.reduce((s, i) => s + i.qty, 0);
  cartCount.textContent = q < 10 ? `0${q}` : q;
}

/* ── Quick View ── */
function getProductTitle(product) {
  if (!product) return "";
  const localizedTitle = loc(product).title;
  if (localizedTitle && localizedTitle.trim()) return localizedTitle.trim();
  return String(product.title || product.name || "").trim();
}

window.openQuickView = function (id, e) {
  if (e) e.stopPropagation();
  const normalizedId = normalizeProductId(id);
  const p = products.find((x) => normalizeProductId(x.id) === normalizedId);
  if (!p) return;
  state.selectedModalProduct = p;
  $("modal-img").src = p.image;
  $("modal-category").textContent = catName(p.category);
  $("modal-title").textContent = getProductTitle(p);
  $("modal-price").textContent = `$${p.price}`;
  $("modal-desc").textContent = loc(p).desc;
  $("modal-qty-val").textContent = modalQty = 1;
  $("modal-badge").classList.toggle("hidden", !p.isSale);
  quickViewModal.classList.remove("hidden");
};
function closeQuickView() {
  quickViewModal.classList.add("hidden");
}

let modalQty = 1;
$("modal-qty-minus")?.addEventListener("click", () => {
  if (modalQty > 1) {
    modalQty--;
    $("modal-qty-val").textContent = modalQty;
  }
});
$("modal-qty-plus")?.addEventListener("click", () => {
  modalQty++;
  $("modal-qty-val").textContent = modalQty;
});
$("modal-add-to-cart-btn")?.addEventListener("click", () => {
  const p = state.selectedModalProduct;
  if (!p) return;
  const title = getProductTitle(p);
  const price = `$${p.price}`;
  const baseMessage = t("wa_msg")
    .replace("{title}", title)
    .replace("{q}", modalQty);
  const message = `${baseMessage}\nPrice: ${price}`;
  const url = `https://wa.me/95898333?text=${encodeURIComponent(message)}`;
  const popup = window.open(url, "_blank", "noopener,noreferrer");
  if (!popup) {
    const a = document.createElement("a");
    a.href = url;
    a.target = "_blank";
    a.rel = "noopener noreferrer";
    document.body.appendChild(a);
    a.click();
    a.remove();
  }
  closeQuickView();
  showToast(t("toast_whatsapp"));
  modalQty = 1;
});

/* ── Cart drawer ── */
function renderCartDrawer() {
  if (state.cart.length === 0) {
    cartItemsContainer.innerHTML = `<div class="empty-state" style="margin:20px 0;"><i class="fa-solid fa-bag-shopping empty-icon"></i><p class="body-md text-muted">${t("cart_empty")}</p></div>`;
    cartSubtotal.textContent = "$0.00";
    checkoutTotal.textContent = "$0.00";
    return;
  }
  let subtotal = 0;
  cartItemsContainer.innerHTML = state.cart
    .map((item) => {
      const p = products.find((x) => x.id === item.id);
      if (!p) return "";
      const total = p.price * item.qty;
      subtotal += total;
      return `<div class="cart-item">
      <img src="${p.image}" class="cart-item-img" alt="${loc(p).title}">
      <div class="cart-item-details">
        <span class="cart-item-title">${loc(p).title}</span>
        <span class="label-sm text-sage">${t("finish_" + item.finish)}</span>
        <span class="cart-item-price">$${p.price} &times; ${item.qty}</span>
        <div class="cart-item-controls">
          <span class="body-md text-flour">$${total}</span>
          <button class="remove-item-btn" onclick="removeFromCart('${item.id}')"><i class="fa-solid fa-trash-can"></i> ${t("remove")}</button>
        </div>
      </div>
    </div>`;
    })
    .join("");
  cartSubtotal.textContent = `$${subtotal}`;
  checkoutTotal.textContent = `$${subtotal}`;
}
window.removeFromCart = function (id) {
  state.cart = state.cart.filter((i) => i.id !== id);
  updateHeaderBadges();
  renderCartDrawer();
  showToast(t("toast_removed_bag"));
};
function openCartDrawer() {
  renderCartDrawer();
  cartDrawer.classList.add("active");
  cartDrawerOverlay.classList.remove("hidden");
}
function closeCartDrawer() {
  cartDrawer.classList.remove("active");
  cartDrawerOverlay.classList.add("hidden");
}

/* ── Toasts ── */
function showToast(msg) {
  const toast = document.createElement("div");
  toast.className = "toast";
  toast.innerHTML = `<i class="fa-solid fa-sparkles"></i> <span>${msg}</span>`;
  toastContainer.appendChild(toast);
  setTimeout(() => {
    toast.style.opacity = "0";
    toast.style.transform = "translateX(100%)";
    setTimeout(() => toast.remove(), 300);
  }, 3000);
}

/* ── Events ─ */
function setupEventListeners() {
  /* Language switcher */
  $("lang-switcher")?.addEventListener("click", (e) => {
    const btn = e.target.closest(".lang-btn");
    if (!btn) return;
    applyLanguage(btn.dataset.lang);
  });
  filterPills?.addEventListener("click", (e) => {
    const btn = e.target.closest(".pill-btn");
    if (!btn) return;
    filterPills
      .querySelectorAll(".pill-btn")
      .forEach((b) => b.classList.remove("active"));
    btn.classList.add("active");
    state.activeCategory = btn.dataset.category;
    renderGrid();
  });
  sortSelect?.addEventListener("change", (e) => {
    state.sortBy = e.target.value;
    renderGrid();
  });
  searchToggleBtn?.addEventListener("click", () => {
    searchOverlay.classList.toggle("active");
    if (searchOverlay.classList.contains("active")) searchInput.focus();
  });
  clearSearchBtn?.addEventListener("click", () => {
    searchInput.value = "";
    state.searchQuery = "";
    renderGrid();
  });
  searchInput?.addEventListener("input", (e) => {
    state.searchQuery = e.target.value;
    renderGrid();
  });
  viewGrid1?.addEventListener("click", () => {
    viewGrid1.classList.add("active");
    viewGrid2.classList.remove("active");
    productGrid.classList.remove("grid-2-col");
    productGrid.classList.add("grid-1-col");
  });
  viewGrid2?.addEventListener("click", () => {
    viewGrid2.classList.add("active");
    viewGrid1.classList.remove("active");
    productGrid.classList.remove("grid-1-col");
    productGrid.classList.add("grid-2-col");
  });
  cartBtn?.addEventListener("click", openCartDrawer);
  closeCartBtn?.addEventListener("click", closeCartDrawer);
  cartDrawerOverlay?.addEventListener("click", closeCartDrawer);
  closeModalBtn?.addEventListener("click", closeQuickView);
  quickViewModal?.addEventListener("click", (e) => {
    if (e.target === quickViewModal) closeQuickView();
  });
  /* Swatches */
  document.querySelectorAll(".swatch-btn").forEach((s) =>
    s.addEventListener("click", () => {
      document
        .querySelectorAll(".swatch-btn")
        .forEach((x) => x.classList.remove("active"));
      s.classList.add("active");
    }),
  );
  /* Mobile nav */
  mobileMenuToggle?.addEventListener("click", () => {
    mobileNavDrawer.classList.add("active");
    mobileNavOverlay.classList.add("active");
  });
  closeMobileNav?.addEventListener("click", () => {
    mobileNavDrawer.classList.remove("active");
    mobileNavOverlay.classList.remove("active");
  });
  mobileNavOverlay?.addEventListener("click", () => {
    mobileNavDrawer.classList.remove("active");
    mobileNavOverlay.classList.remove("active");
  });
  $("reset-filter-btn")?.addEventListener("click", () => {
    state.activeCategory = "all";
    state.searchQuery = "";
    if (searchInput) searchInput.value = "";
    filterPills
      .querySelectorAll(".pill-btn")
      .forEach((b, i) => b.classList.toggle("active", i === 0));
    renderGrid();
  });
  $("newsletter-form")?.addEventListener("submit", (e) => {
    e.preventDefault();
    showToast(t("toast_newsletter"));
    e.target.reset();
  });
}

document.addEventListener("DOMContentLoaded", () => {
  applyLanguage(state.lang); /* Arabic first, with RTL */
  updateHeaderBadges();
  setupEventListeners();
  loadProducts();
});
